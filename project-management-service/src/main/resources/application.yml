eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defautltZone: http://localhost:8989/eureka
application:
  config:
    taches-url: http://localhost:8070


server:
      port: 8088
spring:
  application:
    name: projets
  config:
    import: optional:configserver:http://localhost:8888
  datasource:
    url: jdbc:mysql://localhost:3306/project_management_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: TAHAtaha
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    locations: classpath:db/migration
    baseline-on-migrate: true


